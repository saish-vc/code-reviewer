{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capability.json",
  "identifier": "default",
  "description": "Default capability for REVU desktop app — grants the main window access to core Tauri APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
